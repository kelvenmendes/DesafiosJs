//4.

let copos = [50, 100, 150];

copos.forEach(copo => {
    console.log(`Este copo tem ${copo}ml de capacidade.`);
});


/* 
1. let copos = [50, 100, 150]; - Essa linha declara uma variável chamada copos e atribui a ela um array contendo os valores 50, 100 e 150.

2. copos.forEach(copo => { - Essa linha usa o método forEach para iterar sobre o array copos, chamando a função passada como argumento (copo) para cada elemento do array.

3. console.log(`Este copo tem ${copo}ml de capacidade.`); - Essa linha usa o método console.log para imprimir a mensagem "Este copo tem (valor de copo)ml de capacidade." para cada elemento do array copos. */